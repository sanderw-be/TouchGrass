name: Development Build

on:
  push:
    branches:
      - feature/*
      - fix/*
      - copilot/*

jobs:
  build_android:
    name: Build Android (development)
    type: build
    params:
      platform: android
      profile: development
